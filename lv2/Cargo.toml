[package]
name = "dm_vibrato"
version = "0.1.0"
edition = "2021"

[dependencies]
lv2 = { git = "https://github.com/davemollen/rust-lv2.git", branch = "master", features = [
    "minimal_plugin",
] }
vibrato = { path = "../vibrato" }

[lib]
crate-type = ["cdylib"]
